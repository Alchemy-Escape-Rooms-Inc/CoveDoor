CoveDoor session log
====================

2026-05-29 — speed up the door
----------------------------------------
Files modified:
  - Code/include/MANIFEST.h

Changes:
  - MOTOR_SPEED: 180 -> 220 (~86% duty) — faster door per user request.
  - Updated COMPONENTS doc comment ("71% / 180/255" -> "86% / 220/255")
    to keep the Grimoire docs consistent with the new value.

Notes:
  - MANIFEST.h is the single source of truth; main.cpp already reads
    manifest::MOTOR_SPEED, so no code change needed.
  - Ramp-up (DOOR_RAMP_UP_MS = 500) and 12s timeout left unchanged.
  - Committed 6181802 and pushed to origin/master.
  - FLASHED to the installed ESP32 (ESP32-D0WD-V3, MAC d4:e9:f4:65:3e:48)
    on COM4. Compile clean (Flash 58.2%, RAM 13.8%); all 4 images
    written + hash-verified.
  - Flashing quirk: esptool auto-reset failed repeatedly with
    "Wrong boot mode detected (0x13)". Had to hold BOOT manually and
    flash with esptool --before no_reset. Likely the GPIO2/GPIO5
    strapping pins + BTS7960 load interfering with download mode.
    For next flash: hold BOOT, tap EN, keep holding BOOT, then run
    esptool with --before no_reset.

2026-04-14 — audit + speed/timeout tune
----------------------------------------
Files modified:
  - Code/include/MANIFEST.h
  - Code/src/main.cpp
  - Code/platformio.ini

Changes:
  - MOTOR_SPEED: 150 -> 180 (~71% duty) — slightly faster door.
  - DOOR_TIMEOUT_MS: 8000 -> 12000 — 12s safety timeout.
  - HEARTBEAT_INTERVAL: 30000 -> 300000 — matches WatchTower 5-min spec.
  - Removed unused EMERGENCY_STOP enum value and its getStateString case.
  - Removed undocumented "CLEAR" publish on /limit topic during state
    changes (not in the @LIMIT_EVENT list in MANIFEST.h).
  - Added truncation warning log in mqtt_callback when payload >= 128 bytes.
  - Fixed stale BTS7960 references in platformio.ini header and
    @RESET:HARDWARE section of MANIFEST.h (now says XY160D).
  - Updated all stale doc comments in MANIFEST.h to reflect new
    180 duty and 12s timeout values.

Known issues / notes:
  - MOTOR_IN1 (GPIO 2) and MOTOR_IN2 (GPIO 5) are strapping pins on
    the regular ESP32. User confirmed the installed board boots
    reliably, so pins were left alone. If the board is ever
    swapped and boot-loops return, the GPIO5_BOOT_CRASH quirk in
    MANIFEST.h documents the fix.
  - TEST_MOTOR command uses blocking delay(2000) — STOP will not
    interrupt the 2s pulses. Acceptable for a maintenance command.
  - No hardware watchdog timer (documented in QUIRK:NO_WATCHDOG).

Next steps:
  - Deploy: commit, push, compile, flash to installed ESP32.
  - Verify door opens/closes smoothly at the new speed.
  - Monitor /log topic for any [TIMEOUT] or [MQTT] truncation warnings.


2026-04-14 — board/driver migration: ESP32-S3 + BTS7960
---------------------------------------------------------
During the deploy pipeline, the connected hardware turned out to be an
ESP32-S3 with a BTS7960 driver, not the regular ESP32 + XY160D that the
repo had been migrated to. User confirmed the field is correct and the
repo was wrong. Reverted the firmware back to BTS7960 (RPWM/LPWM) and
ported it to ESP32-S3 on Arduino core 3.x.

Files modified:
  - Code/src/main.cpp — rewritten for BTS7960 + ledcAttach(pin,...) API
  - Code/include/MANIFEST.h — pin map, board, wiring, quirks, components
  - Code/platformio.ini — board=esp32-s3-devkitc-1, unpinned platform,
    kept -std=gnu++17

Pin map (final, installed S3 board):
  RPWM         = GPIO 2
  LPWM         = GPIO 5
  LIMIT_OPEN   = GPIO 16
  LIMIT_CLOSED = GPIO 17   (was GPIO 32 — doesn't exist on S3)

Preserved audit fixes from the earlier session:
  - MOTOR_SPEED 180 (~71% duty)
  - DOOR_TIMEOUT_MS 12000 (12s safety)
  - HEARTBEAT_INTERVAL 300000 (WatchTower spec)
  - No EMERGENCY_STOP enum value
  - No "CLEAR" publish on /limit topic
  - MQTT payload truncation warning

Build + deploy:
  - Compile: PASS (Flash 26.9% of 3.34MB, RAM 13.7%)
  - Flash:   PASS on COM16 (CH343 USB-serial, ESP32-S3 QFN56 rev v0.2,
             8MB flash, 8MB PSRAM detected)

Known issues / notes:
  - LIMIT_CLOSED physical wire must be on GPIO 17 on the S3 breakout.
    If the field wire is still on an old pin, the closed-limit will
    never trigger and the 12s safety timeout will catch every close.
  - QUIRK:BOARD_HISTORY in MANIFEST.h now records that this prop has
    bounced between boards/drivers; future editors beware.


2026-04-14 — reverted to regular ESP32 + BTS7960
--------------------------------------------------
User decided to revert the hardware back to regular ESP32 + BTS7960
(the pin map from commit 5c42e5a) and re-wire accordingly. Firmware
updated to match.

Files modified:
  - Code/src/main.cpp — switched back to ledcSetup/ledcAttachPin
    (Arduino core 2.x API), board header text back to "ESP32"
  - Code/include/MANIFEST.h — @BOARD back to ESP32-DevKitC, PWM channel
    constants restored, LIMIT_CLOSED=GPIO 32, wiring/quirks reverted
  - Code/platformio.ini — board=esp32dev, platform=espressif32@6.9.0
    (pinned for Arduino core 2.x ledcSetup API)

Pin map (final, installed regular ESP32):
  RPWM         = GPIO 2
  LPWM         = GPIO 5
  LIMIT_OPEN   = GPIO 16
  LIMIT_CLOSED = GPIO 32

Preserved audit fixes:
  - MOTOR_SPEED 180, DOOR_TIMEOUT_MS 12000, HEARTBEAT_INTERVAL 300000
  - No EMERGENCY_STOP enum, no /limit CLEAR publish
  - MQTT payload truncation warning

Build + deploy:
  - Compile: PASS (Flash 58.2%, RAM 13.8%)
  - Flash:   SKIPPED — CP210x COM5 on this PC returns Access Denied
             even with the driver reporting ProblemCode 0. No obvious
             process holding the port (Arduino IDE, pio monitor, PuTTY
             all absent). Likely a stale Windows USB handle; suggested
             workarounds: disable/re-enable in Device Manager, try a
             different USB port, or reboot. Firmware is committed and
             ready to flash manually later.

Next steps:
  - Flash firmware.bin to the regular ESP32 manually once the port
    cooperates (path: Code/.pio/build/esp32/firmware.bin).
  - Send PING on MermaidsTale/CoveDoor/command to confirm PONG.
  - Send TEST_MOTOR to verify RPWM and LPWM both drive the motor.


============================================
SESSION: 2026-07-29 — Full /audit (Claude)
============================================
Pulled origin/master: already up to date at f3070bd (v1.2.0).
Files reviewed IN FULL: Code/src/main.cpp, Code/include/MANIFEST.h,
Code/platformio.ini, log.txt, repo layout.

FINDINGS (see conversation for full report):
CRITICAL:
  - TEST_MOTOR (main.cpp:474-486) does not monitor limit switches DURING
    the 2s pulses (only pre-checks) — mid-travel test can drive the door
    into the end stop at duty 220; printed pinion strip risk. No ramp.
WARNINGS:
  - MANIFEST.h:267,274 still say "duty 180" (MOTOR_SPEED is 220)
  - TEST_MOTOR + TESTING status missing from manifest @COMMAND/@STATUS_MSG
  - MANIFEST.h:62 @REPO points to CoveSlidingDoor (actual: CoveDoor.git)
  - Manifest claims ramp-DOWN (lines 56-57, 114-115); code only ramps up
  - Duplicate status publishes: direct send_status + loop state-change
    block double-publish STOPPED/OPENING/CLOSING; ONLINE x2 at boot
SUGGESTIONS: no .gitignore (Code/.pio untracked); magic numbers in
  main.cpp violate manifest Rule 5; unreachable-but-keepable safety net
  at main.cpp:613-621; unused <cstdint>; stale LAST UPDATED + timeout
  wording; empty zigbee-devices/ dir.
MQTT PROTOCOL: PASS (documented quirk: replies on /command, guarded).
UI: N/A.

Next steps: user to pick fixes; top candidate = limit-aware TEST_MOTOR
pulse loop (check limits every ~10ms during pulse).

--- 2026-07-29 (cont.) — FIX ALL audit findings → v1.3.0 ---
main.cpp:
  - TEST_MOTOR now limit-aware DURING pulses: new runTestPulse() ramps up
    like a normal move, polls both switches every 10ms (raw read = ~1-tick
    stop), max 2s per direction; ends by syncing state to physical
    position and refreshing /status (no more stuck-on-TESTING).
  - Deduped /status publishes: loop() state-change block is now the single
    publisher for state transitions (removed direct sends in STOP handler,
    timeout path, startOpening/startClosing, TEST_MOTOR tail, and the
    second ONLINE in setup). ALREADY_OPEN/ALREADY_CLOSED/TESTING unchanged.
  - All magic numbers moved to MANIFEST.h per its Rule 5: SERIAL_BAUD,
    BOOT_SETTLE_MS, WIFI_CONNECT_ATTEMPTS, WIFI_ATTEMPT_DELAY_MS,
    MQTT_BUFFER_SIZE, MQTT_TOPIC_PREFIX, TEST_PULSE_MS, TEST_PAUSE_MS,
    LOOP_TICK_MS, RESET_FLUSH_DELAY_MS.
  - Commented the deliberate safety net in checkLimitSwitches().
MANIFEST.h:
  - FIRMWARE_VERSION 1.2.0 -> 1.3.0; LAST UPDATED 2026-07-29.
  - Fixed @REPO URL (CoveSlidingDoor -> CoveDoor), stale duty 180 -> 220,
    removed false ramp-down claims, corrected timeout log wording.
  - Documented TEST_MOTOR (@COMMAND, @OPERATION, @TEST:STEP9) and TESTING
    (@STATUS_MSG); noted v1.3.0 single-publish-per-transition behavior.
  - Removed unused <cstdint>; added all new constants with @tags.
Repo: added .gitignore (.pio/, .vscode/); removed empty zigbee-devices/.
Build: PASS (pio run — Flash 58.3%, RAM 13.8%).
NOTE: mounted board still runs v1.2.0 — flash pending. WatchTower note:
  v1.3.0 publishes ONE status per transition (was double) — verify nothing
  depended on the duplicates before deploying.
